# CSE Residual Risks

## 2026-06-03 Word/PPT Agent MVP

### Focused Agent E2E Closed

- Command: `pnpm exec playwright test tests/e2e/agent-word-ppt.spec.ts --reporter=list`
- Result time: 2026-06-03 04:28-04:55 +08:00
- Result: failed.
- Evidence:
  - Word and PPT tests both timed out in `onCreateNew()` with `X2T initialization timeout after 300000ms`.
  - Failure occurred before Word/PPT Agent bridge actions ran.
- Classification: sensor/runtime limitation in the current WSL dev-server run, not evidence that Word/PPT Agent save works.

- Re-test command: `APP_URL=http://127.0.0.1:5173 timeout 900s pnpm exec playwright test tests/e2e/agent-word-ppt.spec.ts --reporter=line --workers=1 --retries=0`
- Result time: 2026-06-03 14:32-14:44 +08:00
- Result: passed twice consecutively before the final readiness-probe diagnostic guard, then passed again after the guard; `2 passed` each run.
- Evidence:
  - Word Agent created DOCX, inserted target text, saved through native export -> x2t -> DOCX download, and asserted `word/document.xml`.
  - PPT Agent created PPTX, added slide/text box, saved through native export -> x2t -> PPTX download, and asserted `ppt/slides/slide*.xml`.
  - The prior first-command flake was fixed by making trusted plugin `ready` depend on a real `callCommand` callback probe.

- Re-test command after visible-edit fixes: `APP_URL=http://127.0.0.1:5173 timeout 900s pnpm exec playwright test tests/e2e/agent-word-ppt.spec.ts --reporter=line --workers=1 --retries=0`
- Result time: 2026-06-03 16:27 +08:00
- Result: passed, `2 passed`.
- Evidence:
  - Focused E2E now uses the wired `window.onCreateNew` application path instead of cache-busted `document.ts` imports, so UI callbacks and FAB hiding are part of the test path.
  - Word insertion now uses trusted plugin `Asc.plugin.executeMethod('PasteText')`, which updates the visible editor canvas and still saves target text into `word/document.xml`.
  - PPT focused E2E still saves target text into `ppt/slides/slide*.xml`.
  - Focused E2E asserts `#fab-menu` is hidden after programmatic document creation.

### Focused Word E2E Re-test

- Command: `pnpm exec playwright test tests/e2e/agent-word-ppt.spec.ts --grep "Word Agent" --retries=0 --reporter=list`
- Result time: 2026-06-03 04:55 +08:00
- Result: failed.
- Evidence:
  - After removing unnecessary x2t init for blank-document creation, stale Vite transform still served old `lib/document.ts` on the bare module path.
  - Cache-busted `lib/document.ts?t=...` served the current source and loaded the Word editor iframe.
  - The next failure was `SyntaxError: The requested module '/lib/agent/bridge.ts' does not provide an export named 'officeBridge'`, caused by stale bare `bridge.ts` transform.
- Classification: dev-server transform-cache limitation. It proves the E2E reached the Word editor, but does not prove Word Agent bridge/save completion.

### Dev Server Sensor Limitation

- Attempted to restart Vite on 5174 and 5173 with `--force`.
- Result: process listened on the port but returned no HTTP first byte; Vite child process entered `D` state during dependency optimization.
- Retried 5173 without `--force` at 2026-06-03 05:08 +08:00.
- Result: Vite printed `ready`, listened on port 5173, but `curl -I --max-time 15 http://127.0.0.1:5173/` timed out with 0 bytes received.
- Action taken: stopped the hung Vite sessions; no Playwright/Vite process remains.
- Required follow-up: rerun focused E2E under a stable native Linux/Docker dev server or a clean Vite cache before claiming Done when items 6, 7, 12, and 13.

### Full Smoke E2E Not Closed

- Command: `APP_URL=http://127.0.0.1:5173 timeout 900s pnpm run test:e2e:smoke`
- Result time: 2026-06-03 14:34-14:39 +08:00
- Result: failed, `11 passed`, `4 failed`.
- Failed cases:
  - `new-docx type and save — capture download via __ooDownloads hook`: timeout waiting for `window.__ooDownloads`.
  - `new-xlsx save — capture download via __ooDownloads hook`: timeout waiting for `window.__ooDownloads`.
  - `new-pptx save — capture download and verify structure`: timeout waiting for `window.__ooDownloads`.
  - `convertLocal produces PDF with LiberationSans font preload`: `convertLocal` returned `[x2t:80] Conversion failed with code 80`.
- Classification:
  - The three save failures are in the legacy smoke path that calls editor `asc_Save(false)` and waits for UI download capture; focused Agent save uses the current native export path and passed.
  - The PDF failure remains outside the claimed Word/PPT Agent DOCX/PPTX OOXML save path.
- Required follow-up: either repair the legacy UI-save smoke path or explicitly replace that smoke sensor with the native-export save sensor before marking the broader goal complete.

- Re-test command: `APP_URL=http://127.0.0.1:5173 timeout 900s pnpm run test:e2e:smoke`
- Result time: 2026-06-03 15:26-15:27 +08:00
- Result: failed, `14 passed`, `1 failed`.
- Evidence:
  - DOCX/XLSX/PPTX smoke now use the native-export save path and all passed with OOXML ZIP assertions.
  - `new-xlsx save` was also run no-retry as a focused sensor and passed, producing `New_Document.xlsx` with `xl/worksheets/sheet1.xml`.
  - The remaining failure is `convertLocal produces PDF with LiberationSans font preload`.
  - PDF probe confirmed `/working/fonts/LiberationSans-Regular.ttf` exists in Emscripten FS, `params.xml` contains `<m_sFontDir>/working/fonts/</m_sFontDir>`, bin -> DOCX succeeds, and DOCX -> PDF still returns `[x2t:80] Conversion failed with code 80`.
- Classification:
  - XLSX first-save flake is closed by waiting for the real spreadsheet full SDK constructor `AscCommonExcel.Cl` before calling `asc_DownloadAs`.
  - PDF conversion remains outside the Word/PPT Agent DOCX/PPTX/XLSX OOXML save claim and cannot be soft-passed without touching x2t/font scope.

- Re-test command after Office-neutral UI copy: `APP_URL=http://127.0.0.1:5173 timeout 900s pnpm run test:e2e:smoke`
- Result time: 2026-06-03 15:46-15:47 +08:00
- Result: failed, `14 passed`, `1 failed`.
- Evidence:
  - DOCX/XLSX/PPTX save smoke stayed green after `Office Agent` title/menu wording changes.
  - The only remaining failure stayed `convertLocal produces PDF with LiberationSans font preload`.
  - Failure stack still asserts `result.ok === true` and receives `false`; earlier PDF probes identify the underlying x2t return as `[x2t:80]`.

### Known Architectural Boundaries

- WSL cold start remains a sensor limitation for x2t/WASM and full smoke E2E.
- PDF editor-menu export remains server-side conversion scope and is not claimed.
- Binary DOC/XLS/PPT write output remains outside browser-local x2t WASM scope and is not claimed.
- Word/PPT frame bridge injection is intentionally minimal vendor touch: one bridge file per editor plus script tag injection.

### Manual `/goal` Review

- Status: not complete.
- Machine sampling at 2026-06-03 15:44 +08:00 generated:
  - `/tmp/document-e2e-run/test-results/manual-review/excel-ui.png`
  - `/tmp/document-e2e-run/test-results/manual-review/word-edited.png`
  - `/tmp/document-e2e-run/test-results/manual-review/ppt-edited.png`
  - `/tmp/document-e2e-run/test-results/manual-review/manual-review-evidence.json`
- Evidence limits:
  - Excel editor loaded and the Agent menu was visible in the screenshot with Office-neutral guidance text.
  - Word insertion and save returned real browser results, but the inserted text was not visibly readable in the screenshot.
  - PPT add-slide/add-text-box and save returned real browser results, but the screenshot rendered the inserted text as square glyphs.
  - This machine sampling does not replace human visual review.
- Review ledger: `docs/superpowers/plans/2026-06-03-word-ppt-agent-mvp-review.md`.
- Required before completion:
  - Excel Agent UI/UX visual check after final build.
  - Word visible edit check in browser.
  - PPT visible edit check in browser.
  - DOCX/PPTX downloaded file format and OOXML content check.
  - Observable error-state check for unsupported/missing runtime APIs.

- Additional machine sampling after visible-edit fixes at 2026-06-03 16:27 +08:00 generated:
  - `/tmp/document-e2e-run/test-results/agent-visual-current/word-current.png`
  - `/tmp/document-e2e-run/test-results/agent-visual-current/ppt-current.png`
  - `/tmp/document-e2e-run/test-results/agent-visual-current/evidence.json`
- Updated evidence:
  - Word and PPT Agent tool calls returned `ok: true`.
  - `#fab-menu` computed as `display: none`, `pointer-events: none`, `opacity: 0`.
  - No `Cannot read properties` warning was visible in the host body text.
  - Word and PPT screenshots show visible inserted glyph runs/regions rather than blank canvases.
- Remaining review limit:
  - The current WSL/headless Chromium font stack renders inserted ASCII text as square glyphs in the editor canvas.
  - This proves visible mutation but does not close the human-readable visual review gate.
